# This is a template for configuration
# Copy this to 'env' for shared default settings
# Then create 'env.local' for your personal settings (this file won't be checked into git)

# Confluence Configuration
CONFLUENCE_URL=https://your-company.atlassian.net/wiki
CONFLUENCE_USERNAME=your.email@domain.com
CONFLUENCE_API_TOKEN=your_api_token_here

# Optional: Space key to use for testing (if not provided when creating pages)
TESTING_SPACE_KEY=~username123456

# Optional: Filter spaces to specific keys (comma-separated)
CONFLUENCE_SPACES_FILTER=SPACE1,SPACE2,~username123456

# Optional: Transport type (default is stdio)
# Set to 'true' to use HTTP transport on port 3002 instead of stdio
USE_HTTP_TRANSPORT=false

# Optionally set if you want to filter for specific spaces
# CONFLUENCE_SPACES_FILTER=DEV,TEAM,DOCS